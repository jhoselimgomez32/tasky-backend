{
    "name": "Tasky API",
    "description": "The API for the Tasky App",
    "keywords": ["java", "spring-boot", "uan"],
    "addons": [
      {
        "plan": "heroku-postgresql",
        "options": {
          "version": "14"
        }
      }
    ],
    "env": {
      "LIQUIBASE_ENABLED": {
        "description": "A secret key to enable Liquibase schema management. Use false locally when using H2 database.",
        "value": "true"
      }
    }
  }
